<!DOCTYPE html>
<html>
<head>

</head>

<body>
	<% if @users.any? %>
		<% @users.each do |user| %>
			<% user.events.each do |event| %>
				<%= link_to event.name, event_path(event.id) %>
			<% end %>
		<% end %>
	<% end %>



</body>

</html>
