<%if @events.any?%>
  <section class="main-form-container">
    <%= render @events.reverse %>
  </section>
<% else %>
  <h3>There are no events at the moment.</h3>
<%end%>
<%= link_to 'Add Your Event', select_events_path %>
