
<%if @events.any?%>
Choose an Event </br>
  <% @events.each do |event| %>
    <%= link_to "#{event['eventName']}", new_event_path(page_short_name: event['pageShortName']) %></br>
  <% end %>
<%else %>
 <%= link_to 'Create Event', new_event_path %>

<% end %>

