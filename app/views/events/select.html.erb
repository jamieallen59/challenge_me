
<%if @events.any?%>
Hello <%= current_user.firstname %></br>
Which of your justgiving events do you want to view? </br>
  <% @events.each_with_index do |event, index| %>
    <%="#{index + 1}."%><%= link_to "#{event['eventName']}", new_event_path(page_short_name: event['pageShortName']) %></br>

  <% end %>
    <%= link_to 'Create new event', new_event_path %>
<%else %>
  <%= link_to 'Create new event', new_event_path %>
<% end %>

